# tendencias2026

Proyecto base de Flask con entorno virtual en `.venv`.

## Ejecutar

1. Activar el entorno virtual:
	- PowerShell: `.\.venv\Scripts\Activate.ps1`
2. Instalar dependencias si hace falta:
	- `pip install -r requirements.txt`
3. Ejecutar la app:
	- `python app.py`
